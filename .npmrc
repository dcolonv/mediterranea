# React Native / Metro (Expo) cannot resolve pnpm's default isolated,
# symlinked node_modules. A hoisted (flat) layout is required for the
# mobile app's Metro bundler to find transitive deps like metro-runtime.
# See: https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
